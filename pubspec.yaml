name: sigaflutter
description: A scaffold class with a sideBar that works with a appBar.
version: 1.1.1
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:

